<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.ethan.pokerjournal.HistoryFragment">

    // New Game Button
    <Button
        android:id="@+id/buttonResetGames"
        android:text="Clear"
        android:onClick="onClickResetGame"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    // List View for Game Session History
    <ListView
        android:id="@+id/listGames"
        android:scrollbars="vertical"
        android:fastScrollEnabled="true"
        android:fastScrollAlwaysVisible="true"
        android:layout_below="@+id/buttonResetGames"
        android:layout_alignEnd="@+id/buttonResetGames"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    // Reset Button
    <Button
        android:id="@+id/buttonNewGame"
        android:text="New Game"
        android:onClick="onClickNewGame"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</RelativeLayout>